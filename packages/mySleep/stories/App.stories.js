import React, { useState, useEffect } from "react";
import MySleep from "../src/MySleep";

export default { title: "MySleep" };

export const app = () => {
  return <MySleep data={{}} />;
};
app.story = {
  name: "MySleep",
};
